#include "Class.h"
#include "Operation.h"

// Main Function
int main()
{
    AVL_Node *root = nullptr;
    BookRecord *head = nullptr;
    BookRecord *tail = nullptr;
    Selection(root, head, tail);

    return 0;
}
